# SMTP Configuration for Email Sending (Server-side only)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@promith.com
SMTP_FROM_NAME=Promith

# Email Recipients
CONTACT_FORM_TO_EMAIL=hello@promith.com

# Next.js Configuration
NODE_ENV=production

# NOTE: All site content is managed in /src/data/content.json
# No CMS required - simply edit the JSON file to update content
